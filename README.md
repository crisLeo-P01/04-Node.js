# Nota
Estos son los comandos para la aplicacion
```
Options:
      --help     Show help                                              [boolean]
      --version  Show version number                                    [boolean]
  -b, --base     Es la base de la tabla de multiplicar                  [number] [required]
  -l, --listar   Muestra el listado de la multiplicación en consola     [boolean] [default: false]
  -h, --hasta    Especifica hasta que multiplo desea multiplicar        [number] [default: 10]
```
